== TODO

* A "worker" is a foreigner entrant who has WORK listed as their purpose on their access permit

== Add test

== To improve

* Add document validation rule?

* change return of check (Optional<String> is weird)

* Regroup tests by type (criminal, vaccination) + test priority/conflicts

* externalize bulletin parsing with design pattern

== To study

== To share

* or et method reference sur string.formatted()
[,java]
----
checkRequiredVaccinationsForAll(entrant)
.or(() -> checkRequiredVaccinationsByCountry(entrant))
.map("Entry denied: missing required %s vaccination."::formatted);
----
* regexp look behind assertion
[,java]
----
public static final Pattern REQUIRED_DOCUMENTS_PATTERN = Pattern.compile("(.*) require (.*)(?<!vaccination)$");
----

